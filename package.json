{
  "name": "furiousacid-static-site",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "audit:purple:mobile": "node scripts/audit-purple-mobile.mjs",
    "mobile:prepare": "node scripts/prepare-capacitor-web.mjs",
    "mobile:check": "node scripts/check-capacitor-mobile.mjs",
    "mobile:add": "npm run mobile:prepare && npx cap add android && npx cap add ios",
    "mobile:sync": "npm run mobile:prepare && npm run mobile:check && npx cap sync",
    "mobile:android": "npm run mobile:sync && npx cap open android",
    "mobile:ios": "npm run mobile:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "8.4.2"
  },
  "devDependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/cli": "8.4.2",
    "@capacitor/ios": "8.4.2",
    "jsdom": "26.1.0",
    "playwright": "1.54.2"
  }
}
